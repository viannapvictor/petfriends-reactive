apiVersion: v1
kind: Service
metadata:
  name: kafka-service
  namespace: petfriends
spec:
  selector:
    app: kafka
  type: ClusterIP
  ports:
  - port: 9093
    targetPort: 9093
    protocol: TCP
    name: internal
  - port: 9092
    targetPort: 9092
    protocol: TCP
    name: external

